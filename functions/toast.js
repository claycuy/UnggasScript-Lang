let normalToast = function normalToast(message) {
  alert(message);
}

function backToast(message) {
  console.log(message);
}

let userInput;
function inputToast(message) {
  userInput = prompt(message);
}